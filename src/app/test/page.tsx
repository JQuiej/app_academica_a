// The component must be the default export
export default function TestPage() {
  // The component must return a valid JSX element (like <main>, <div>, etc.)
  return (
    <main>
      <h1>Test Page</h1>
      <p>If you can see this, the component is working correctly.</p>
    </main>
  );
}